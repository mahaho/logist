# Система учёта работы транспортной компании

Полнофункциональная система управления транспортной компанией с разделением ролей, учётом рейсов, транспорта, финансов и документов.

## 🚀 Технологический стек

### Backend
- **Node.js** + **Express** - серверная часть
- **PostgreSQL** - база данных
- **Prisma** - ORM
- **JWT** - аутентификация
- **Zod** - валидация
- **Winston** - логирование
- **Multer** - загрузка файлов

### Frontend
- **React** + **TypeScript** - пользовательский интерфейс
- **Tailwind CSS** - стилизация
- **React Router** - маршрутизация
- **Zustand** - управление состоянием
- **Axios** - HTTP клиент
- **Vite** - сборщик

### Тестирование
- **Vitest** - тестовый фреймворк
- **Supertest** - тестирование API

## 📋 Функциональность

### Роли пользователей
- **admin** - полный доступ
- **dispatcher** - рейсы, сцепки, маршруты, документы рейсов
- **accountant** - финансы, расходы, доходы, отчёты
- **mechanic** - ТО, ремонты, тех. документы

### Модули системы

1. **Транспорт**
   - Тягачи (Tractors)
   - Прицепы (Trailers)
   - Сцепки (Couplings)

2. **Рейсы (Trips)**
   - Создание и управление рейсами
   - Автоматический расчёт суммы (тонны × ставка)
   - Статусы: planned, in_progress, completed, cancelled
   - Привязка к водителю, тягачу и прицепу

3. **ТО и ремонты (Maintenance)**
   - Плановые и внеплановые работы
   - Учёт материалов и затрат
   - История обслуживания

4. **Документы**
   - Документы водителей
   - Документы транспорта
   - Документы рейсов
   - Напоминания о сроках действия

5. **Финансы**
   - Типизированные операции (топливо, мойка, парковка, суточные, аванс, зарплата, ремонт, ТО, платные дороги, прочее)
   - Связь с рейсами, водителями, транспортом
   - Контроль остатков

6. **Отчёты**
   - Финансовые отчёты (по рейсу, водителю, транспорту, компании)
   - Реестр рейсов
   - История ТО
   - Нагрузка водителей
   - Истекающие и просроченные документы

## 🛠️ Установка и запуск

### Требования
- Node.js 18+
- PostgreSQL 14+
- npm или yarn

### 1. Клонирование и установка зависимостей

```bash
# Установка зависимостей корневого проекта
npm install

# Установка зависимостей backend
cd backend
npm install

# Установка зависимостей frontend
cd ../frontend
npm install
```

### 2. Настройка базы данных

1. Создайте базу данных PostgreSQL:
```sql
CREATE DATABASE logist_db;
```

2. Настройте переменные окружения в `backend/.env`:
```env
DATABASE_URL="postgresql://user:password@localhost:5432/logist_db?schema=public"
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
JWT_EXPIRES_IN="7d"
PORT=3001
NODE_ENV=development
UPLOAD_DIR="./uploads"
MAX_FILE_SIZE=10485760
```

3. Запустите миграции Prisma:
```bash
cd backend
npx prisma migrate dev
npx prisma generate
```

### 3. Создание начальных данных

Запустите seed скрипт для создания тестовых пользователей:

```bash
cd backend
npm run prisma:seed
```

Это создаст пользователей с ролями:
- **admin@logist.ru** / admin123 (Администратор)
- **dispatcher@logist.ru** / dispatcher123 (Диспетчер)
- **accountant@logist.ru** / accountant123 (Бухгалтер)
- **mechanic@logist.ru** / mechanic123 (Механик)

Вы также можете создать пользователей через Prisma Studio:

```bash
cd backend
npx prisma studio
```

### 4. Запуск приложения

#### Вариант 1: Запуск через корневой package.json (рекомендуется)
```bash
# Из корневой директории
npm run dev
```

Это запустит одновременно backend (порт 3001) и frontend (порт 3000).

#### Вариант 2: Запуск отдельно

**Backend:**
```bash
cd backend
npm run dev
```

**Frontend:**
```bash
cd frontend
npm run dev
```

### 5. Доступ к приложению

- Frontend: http://localhost:3000
- Backend API: http://localhost:3001
- Prisma Studio: `cd backend && npx prisma studio`

## 📁 Структура проекта

```
.
├── backend/
│   ├── src/
│   │   ├── controllers/     # Контроллеры API
│   │   ├── middleware/      # Middleware (auth, error handling, upload)
│   │   ├── routes/          # Маршруты API
│   │   ├── utils/           # Утилиты (logger, prisma, pagination)
│   │   ├── __tests__/       # Тесты
│   │   └── index.ts         # Точка входа
│   ├── prisma/
│   │   └── schema.prisma    # Схема базы данных
│   ├── uploads/             # Загруженные файлы
│   └── package.json
├── frontend/
│   ├── src/
│   │   ├── components/      # React компоненты
│   │   ├── pages/          # Страницы приложения
│   │   ├── store/          # Zustand stores
│   │   ├── lib/            # Утилиты (API client)
│   │   └── App.tsx         # Главный компонент
│   └── package.json
└── package.json            # Корневой package.json (workspaces)
```

## 🧪 Тестирование

```bash
# Backend тесты
cd backend
npm test

# Frontend тесты
cd frontend
npm test
```

## 📝 API Endpoints

### Аутентификация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Текущий пользователь

### Пользователи (admin only)
- `GET /api/users` - Список пользователей
- `GET /api/users/:id` - Получить пользователя
- `POST /api/users` - Создать пользователя
- `PUT /api/users/:id` - Обновить пользователя
- `DELETE /api/users/:id` - Удалить пользователя

### Водители
- `GET /api/drivers` - Список водителей
- `GET /api/drivers/:id` - Получить водителя
- `POST /api/drivers` - Создать водителя
- `PUT /api/drivers/:id` - Обновить водителя
- `DELETE /api/drivers/:id` - Удалить водителя

### Тягачи
- `GET /api/tractors` - Список тягачей
- `GET /api/tractors/:id` - Получить тягач
- `POST /api/tractors` - Создать тягач
- `PUT /api/tractors/:id` - Обновить тягач
- `DELETE /api/tractors/:id` - Удалить тягач

### Прицепы
- `GET /api/trailers` - Список прицепов
- `GET /api/trailers/:id` - Получить прицеп
- `POST /api/trailers` - Создать прицеп
- `PUT /api/trailers/:id` - Обновить прицеп
- `DELETE /api/trailers/:id` - Удалить прицеп

### Сцепки
- `GET /api/couplings` - Список сцепок
- `GET /api/couplings/:id` - Получить сцепку
- `POST /api/couplings` - Создать сцепку
- `PUT /api/couplings/:id` - Обновить сцепку
- `DELETE /api/couplings/:id` - Удалить сцепку

### Рейсы
- `GET /api/trips` - Список рейсов
- `GET /api/trips/:id` - Получить рейс
- `POST /api/trips` - Создать рейс
- `PUT /api/trips/:id` - Обновить рейс
- `DELETE /api/trips/:id` - Удалить рейс

### ТО / Ремонты
- `GET /api/maintenance` - Список записей
- `GET /api/maintenance/:id` - Получить запись
- `POST /api/maintenance` - Создать запись
- `PUT /api/maintenance/:id` - Обновить запись
- `DELETE /api/maintenance/:id` - Удалить запись

### Документы
- `GET /api/documents` - Список документов
- `GET /api/documents/:id` - Получить документ
- `GET /api/documents/:id/download` - Скачать документ
- `POST /api/documents` - Загрузить документ (multipart/form-data)
- `PUT /api/documents/:id` - Обновить документ
- `DELETE /api/documents/:id` - Удалить документ

### Финансы
- `GET /api/finance` - Список операций
- `GET /api/finance/:id` - Получить операцию
- `POST /api/finance` - Создать операцию
- `PUT /api/finance/:id` - Обновить операцию
- `DELETE /api/finance/:id` - Удалить операцию

### Отчёты
- `GET /api/reports/finance/trip/:tripId` - Прибыль/убыток по рейсу
- `GET /api/reports/finance/driver/:driverId` - Финансовый отчёт по водителю
- `GET /api/reports/finance/tractor/:tractorId` - Расходы по тягачу
- `GET /api/reports/finance/trailer/:trailerId` - Расходы по прицепу
- `GET /api/reports/finance/company` - Общий финансовый отчёт
- `GET /api/reports/trips/register` - Реестр рейсов
- `GET /api/reports/maintenance/history` - История ТО
- `GET /api/reports/maintenance/upcoming` - Предстоящие ТО
- `GET /api/reports/drivers/workload` - Нагрузка водителей
- `GET /api/reports/documents/expiring` - Истекающие документы
- `GET /api/reports/documents/expired` - Просроченные документы

## 🔒 Безопасность

- JWT токены для аутентификации
- Хеширование паролей с bcrypt
- Ролевой контроль доступа (RBAC)
- Валидация входных данных (Zod)
- Обработка ошибок

## 📦 Production сборка

```bash
# Backend
cd backend
npm run build
npm start

# Frontend
cd frontend
npm run build
# Разверните dist/ на веб-сервере
```

## 🐛 Решение проблем

### Проблемы с базой данных
- Убедитесь, что PostgreSQL запущен
- Проверьте DATABASE_URL в .env
- Запустите миграции: `npx prisma migrate dev`

### Проблемы с авторизацией
- Проверьте JWT_SECRET в .env
- Убедитесь, что токен передаётся в заголовке Authorization

### Проблемы с загрузкой файлов
- Убедитесь, что папка uploads существует
- Проверьте права доступа к папке

## 📄 Лицензия

MIT

## 👥 Автор

Система разработана для управления транспортной компанией.

---

**Примечание:** Для production использования обязательно:
1. Измените JWT_SECRET на надёжный ключ
2. Настройте HTTPS
3. Используйте переменные окружения для всех секретов
4. Настройте резервное копирование базы данных
5. Настройте мониторинг и логирование

